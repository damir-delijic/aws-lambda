define("libraryManager", ["Backbone", "underscore", "jquery", "text", "json"], function (Backbone, _, $, text, json) {

    return {
        "Backbone": Backbone,
        "_": _,
        "$": $,
        "text": text,
        "json": json
    }

});